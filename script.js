const header=document.querySelector('.site-header');const button=document.querySelector('.menu-button');button?.addEventListener('click',()=>{const open=header.classList.toggle('nav-open');button.setAttribute('aria-expanded',open)});document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>{header?.classList.remove('nav-open');button?.setAttribute('aria-expanded','false')}));const documentInput=document.querySelector('#document');const uploadTitle=document.querySelector('#uploadTitle');const uploadHint=document.querySelector('#uploadHint');documentInput?.addEventListener('change',()=>{const file=documentInput.files[0];if(!file)return;if(file.size>20*1024*1024){uploadTitle.textContent='That file is larger than 20 MB';uploadHint.textContent='Please choose a smaller file.';documentInput.value='';return}uploadTitle.textContent=file.name;uploadHint.textContent='Document attached and ready to send.'});document.querySelector('#consultForm')?.addEventListener('submit',event=>{event.preventDefault();document.querySelector('#success')?.classList.add('show');event.currentTarget.querySelector('.submit').textContent='Request prepared ✓'});
